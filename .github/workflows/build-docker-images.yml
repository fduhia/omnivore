name: Run tests
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'apple/**'
      - 'android/**'

jobs:
  build-docker-images:
    name: Build docker images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: 'Login to GitHub container registry'
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
      - name: Build the backend docker image
        env:
          IMAGE_NAME: ${{ github.repository }}
        run: |
          docker build . --file packages/api/Dockerfile --tag "${{ registry }}/${{ env.IMAGE_NAME}}/omnivore-backend:${GITHUB_SHA}" --tag ${{ registry }}/${{ env.IMAGE_NAME}}/omnivore-backend:latest
          docker push ghcr.io/omnivore-app/backend:${GITHUB_SHA}
      - name: Build the content-fetch docker image
        run: |
          docker build --file packages/content-fetch/Dockerfile . --tag "${{ registry }}/${{ env.IMAGE_NAME}}/omnivore-content-fetch:${GITHUB_SHA}" --tag ${{ registry }}/${{ env.IMAGE_NAME}}/omnivore-content-fetch:latest
          docker push ${{ registry }}/${{ env.IMAGE_NAME}}/omnivore-content-fetch:${GITHUB_SHA}
